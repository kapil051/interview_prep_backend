package com.interviewprep.backend.module.questions.blind75.enums;

public enum Pattern {
    HASHMAP, HASHSET, TWO_POINTERS, SLIDING_WINDOW, FAST_SLOW_POINTERS,
    BINARY_SEARCH, DFS, BFS, DYNAMIC_PROGRAMMING, BACKTRACKING, GREEDY,
    STACK, HEAP, TRIE
}
